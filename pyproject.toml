[project]
name = "djangotut"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Yaroslav Filinchuk", email = "filinchuk230804@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "crispy-bootstrap4>=2026.2",
    "django>=6.0.7",
    "django-crispy-forms>=2.7",
]

[project.scripts]
djangotut = "djangotut:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
